@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" asp-page="/AddBook" >Add <span class="sr-only">(books)</span></a>
            </li>
           
        </ul>
        <form method="post" asp-page-handler="Searhc" class="form-inline my-0 my-lg-0">
            <input name="PoickName" class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
            
        </form>
    </div>
</nav>
@if (Model.books2!=null)
{
    <table class="table table-dark">
    <thead>
        <tr>
                <th scope="col">Name product</th>
                <th scope="col">Number of pieces</th>
                <th scope="col">Price</th>

           
            @*<th scope="col">BookName</th>
            <th scope="col">AuthorName</th>
            <th scope="col">Style</th>
            <th scope="col">Publisher</th>
            <th scope="col">Year</th>*@
            
        </tr>
    </thead>
    
        <tbody>
        @foreach (var item in Model.books2)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.AuthoName</td>
                <td>@item.Publisher</td>
                   @* <td>@item.Name</td>
                    <td>@item.AuthoName</td>
                    <td>@item.Style</td>
                    <td>@item.Publisher</td>
                    <td>@item.Year</td>*@
            </tr>
        }
        </tbody>
    </table>
}
<table class="table table-dark">
    <thead>
        <tr>
            @*<th scope="col">Id</th>
            <th scope="col">BookName</th>
            <th scope="col">AuthorName</th>
            <th scope="col">Style</th>
            <th scope="col">Publisher</th>
            <th scope="col">Year</th>
            <th>Info</th>
            <th>Edit</th>
            <th>Delete</th>*@
            <th scope="col">Name product</th>
            <th scope="col">Number of pieces</th>
            <th scope="col">Price</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.books)
        {
            <tr>
                @*<td>@item.Id</td>
                <td>@item.Name</td>
                <td>@item.AuthoName</td>
                <td>@item.Style</td>
                <td>@item.Publisher</td>
                <td>@item.Year</td>*@
                <td>@item.Name</td>
                <td>@item.AuthoName</td>
                <td>@item.Publisher</td>
               @* <td>
                    <form asp-page="Info" method="GET" asp-route-id="@item.Id">
                        <button type="submit">Info</button>
                    </form>
                </td>
                <td>
                    <form asp-page="Edit" method="get" asp-route-id="@item.Id">
                        <button>Edit</button>
                    </form>
                </td>
                <td>
                    <form asp-page-handler="Delete" asp-route-id="@item.Id">
                        <button>Delete</button></form>
                </td>
*@
            </tr>



       }
    </tbody>
</table>

